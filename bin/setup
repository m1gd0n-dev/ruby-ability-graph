#!/usr/bin/env bash
set -euo pipefail
IFS=$'\n\t' # nosemgrep: bash.lang.security.ifs-tampering.ifs-tampering -- standard "safe bash" idiom; no unquoted expansion of external input below

bundle install
